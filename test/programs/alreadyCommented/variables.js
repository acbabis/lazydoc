var a; // declare a
var b = 5; // declare and set b to 5
var c = 1E10; // declare and set c to 1E10
var d = 'String'; // declare and set d to 'String'
var e = 'string with spaces'; // declare and set e to 'string with spaces'

var longVariableName; // declare longVariableName
var longVariableName = 100; // declare and set longVariableName to 100
var longVariableName = 'str'; // declare and set longVariableName to 'str'

var f = new Date(); // declare and set f to new Date()
var g = 5 + 7; // declare and set g to 5 + 7
var h = 'asdf' + 5 + new Date() + asdf.getASADF(); // declare and set h to 'asdf' + 5 + new Date() + asdf.getASADF()

var i, // declare i, and 
    j, // declare j, and 
    k = 7, // declare and set k to 7, and 
    l, // declare l, and 
    m = 100; // declare and set m to 100